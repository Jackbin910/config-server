server:
  port: 9100

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://code.aliyun.com/jackbin/config-cloud.git
          username: jackbin
          password: jackbin91
